<form method="post" action="{{ path('contact_pages_delete', {'id': contact_page.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_page.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
