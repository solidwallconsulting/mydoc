 

{% extends 'admin-base.html.twig' %}


{% block body %}


	<main class="main-content">
		{% block topNav %}
			{{ parent() }}
		{% endblock %}

		<div class="conatiner-fluid content-inner mt-n5 py-0">
			<div class="row">

				<div class="col-12">
					<div class="card">
						<div class="card-header d-flex justify-content-between flex-wrap">
							<div class="header-title">
								<h4 class="card-title">Mes message</h4>
							</div>

						 

						</div>
						<div class="card-body w-100">


							<table class="table datatable">
		<thead>
			<tr>
				<th>#</th>
				<th>Nom & prénom</th>
				<th>Email</th>
				<th>Sujet</th> 
				<th>Date </th> 
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for message in messages %}
				{% if message.isSeen == false %}
                    <tr>
					<td>{{ loop.index }}</td>
					<td>
                        <strong>{{ message.name }}</strong>
                    </td>
					<td><strong>{{ message.email }}</strong></td>
					<td>{{ message.subject }}</td> 
					<td>{{ message.sentAt ? message.sentAt|date('Y-m-d H:i:s') : '' }}</td>
					
					<td>
						<a href="{{ path('message_show', {'id': message.id}) }}">Afficher</a> 
					</td>
				</tr>
			{% else %}
				 
                {% endif %}
			{% endfor %}


            {% for message in messages %}
				{% if message.isSeen == true %}
                    <tr>
					<td>{{ loop.index }}</td>
					<td>
                        <stron>{{ message.name }}</strong>
                    </td>
					<td>{{ message.email }}</td>
					<td>{{ message.subject }}</td> 
					<td>{{ message.sentAt ? message.sentAt|date('Y-m-d H:i:s') : '' }}</td>
					
					<td>
						<a href="{{ path('message_show', {'id': message.id}) }}">Afficher</a> 
					</td>
				</tr>
			{% else %}
				 
                {% endif %}
			{% endfor %}
		</tbody>
	</table>

						</div>


					</div>

				</div>


			{% endblock %}
