{% extends 'base.html.twig' %}

{% block title %}ContractPageProperty{% endblock %}

{% block body %}
    <h1>ContractPageProperty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contract_page_property.id }}</td>
            </tr>
            <tr>
                <th>FeildName</th>
                <td>{{ contract_page_property.feildName }}</td>
            </tr>
            <tr>
                <th>FeildID</th>
                <td>{{ contract_page_property.feildID }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ contract_page_property.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contract_page_property_index') }}">back to list</a>

    <a href="{{ path('contract_page_property_edit', {'id': contract_page_property.id}) }}">edit</a>

    {{ include('contract_page_property/_delete_form.html.twig') }}
{% endblock %}
